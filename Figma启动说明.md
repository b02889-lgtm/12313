# Figma MCP 服务器启动指南

## 启动方式

### 方式一：后台启动（推荐）
双击运行 `start_figma_mcp_background.bat`
- 服务器在后台运行
- 可以关闭启动窗口，服务器继续运行
- 端口：8080

### 方式二：静默启动（无窗口）
双击运行 `start_figma_mcp_service.vbs`
- 完全在后台运行，不显示任何窗口
- 适合开机自动启动
- 端口：8080

### 方式三：前台启动（调试用）
双击运行 `start_figma_mcp.bat`
- 在前台运行，可以看到日志输出
- 适合调试和查看连接状态
- 端口：8080

## 停止服务器

双击运行 `stop_figma_mcp.bat` 即可停止服务器。

## 开机自动启动

如果希望开机自动启动 Figma MCP 服务器：

1. 按 `Win + R`，输入 `shell:startup`，回车
2. 将 `start_figma_mcp_service.vbs` 的快捷方式复制到打开的文件夹中
3. 重启电脑后，服务器会自动启动

## 验证服务器状态

服务器启动后，可以通过以下方式验证：
- 访问 http://127.0.0.1:8080/sse
- 或在 VSCode 中使用 Figma MCP 功能

## 注意事项

- 确保端口 8080 未被其他程序占用
- 如果需要更改端口，请修改启动脚本中的 `--port` 参数
- API Key 已配置在脚本中，无需额外设置
